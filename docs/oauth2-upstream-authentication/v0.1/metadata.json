{
  "name": "oauth2-upstream-authentication",
  "displayName": "OAuth2 Upstream Authentication",
  "version": "1.0",
  "provider": "WSO2",
  "categories": [
    "Security",
    "AI"
  ],
  "description": "Authenticates outbound requests to an upstream backend using OAuth2. The gateway\nexchanges credentials for a short-lived access token at a configured token endpoint\nand injects it as an Authorization: Bearer header. Tokens are cached and refreshed\nautomatically ahead of expiry. The grant used is selected via grantType; only\nclient_credentials (RFC 6749 Section 4.4) is implemented today."
}
