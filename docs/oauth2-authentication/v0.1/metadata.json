{
  "name": "oauth2-authentication",
  "displayName": "OAuth2 Authentication",
  "version": "1.0",
  "provider": "WSO2",
  "categories": [
    "Security",
    "AI"
  ],
  "description": "Authenticates outbound requests to an upstream backend using the OAuth2 Client\nCredentials grant (RFC 6749 Section 4.4). The gateway exchanges a client ID and\nsecret for a short-lived access token at a configured token endpoint and injects\nit as an Authorization: Bearer header. Tokens are cached and refreshed\nautomatically ahead of expiry."
}
