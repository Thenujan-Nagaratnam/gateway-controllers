# Headroom Compressor Policy

`headroom-compressor` is a Python policy package that compresses the request's
message history via [Headroom](https://github.com/headroomlabs-ai/headroom)
just before the upstream LLM call.
